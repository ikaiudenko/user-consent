export enum ValidationErrors {
  Fail = "API_VALIDATION_FAIL",
}

export enum UserErrors {
  Create = "USER_CREATE_FAIL",
  Delete = "USER_DELETE_FAIL",
}
export enum TokenErrors {
  Create = "TOKEN_CREATE_FAIL",
}

export enum AuthErrors {
  BadToken = "INVALID_USER_TOKEN",
  BadApiToken = "INVALID_API_TOKEN",
}
